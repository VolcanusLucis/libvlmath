name: Tagged Release Workflow
permissions:
  contents: write

on:
  push:
    tags:
      - "v*"

jobs:
  check-valid-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Check Valid Branch
        run: |
          branches=$(git branch -r --contains tags/${{ github.ref_name }})
          for branch in $branches; do
            if [[ $branch == "origin/master" ]]; then
              return 0;
            else
              return 1;
            fi
          done

  create-release:
    needs: [ check-valid-branch ]
    runs-on: ubuntu-latest

    steps:
      - name: Create Release
        uses: VolcanusLucis/.github/.github/actions/create-release@master
        with:
          tag: ${{ github.ref_name }}
          draft: 'true'
          token: ${{ secrets.GITHUB_TOKEN }}

  build-releases:
    needs: [ check-valid-branch, create-release ]
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      
      matrix:
        target: [ win, linux ]
        architecture: [ x64, x86 ]
        compiler: [ mingw64, gcc ]
        build-type: [ release ]
        exclude:
          - target: win
            compiler: gcc
          - target: linux
            compiler: mingw64

    steps:
      - name: Build
        uses: VolcanusLucis/.github/.github/actions/build-cmake-project-presets@master
        with:
          target: ${{ matrix.target }}
          architecture: ${{ matrix.architecture }}
          compiler: ${{ matrix.compiler }}
          build-type: ${{ matrix.build-type }}

      - name: Install
        if: success()
        shell: bash
        run: cmake --install "build/${{ matrix.target }}-${{ matrix.architecture }}-${{ matrix.compiler }}" 
          --prefix "install/${{ matrix.target }}-${{ matrix.architecture }}-${{ matrix.compiler }}"
          --config ${{ matrix.build-type }}

      - name: Package
        shell: bash
        run: tar -C install/ -czf libvlmath-${{ matrix.target }}-${{ matrix.architecture }}-${{ matrix.compiler }}.tar.gz
          ${{ matrix.target }}-${{ matrix.architecture }}-${{ matrix.compiler }}

      - name: Upload
        uses: VolcanusLucis/.github/.github/actions/upload-to-release@master
        with:
          tag: ${{ github.ref_name }}
          file: 'libvlmath-${{ matrix.target }}-${{ matrix.architecture }}-${{ matrix.compiler }}.tar.gz'
          token: ${{ secrets.GITHUB_TOKEN }}